# HackProduct handoff — September 6, 2026

## Start here

The user is transferring this work to Grok or Claude because the current session has taken too long and is approaching usage limits. Finish the remaining work with focused changes; do not restart broad redesign, repeat passed journeys, or run another paid canary without diagnosing the recorded failure first.

- Repository: `/Users/sandeep/Projects/myproductschool`
- GitHub: `sunboy/myproductschool`
- Working branch: `feat/platform-rebuild-20260905`
- Draft PR: https://github.com/sunboy/myproductschool/pull/20 — open, unmerged.
- Most recent tested runtime commit: `7e3c4d2822abee64560fc9db3d12734729618457`.
- Its READY deployment: `dpl_4gzMyq6K7V9xeAxaS5C8qHfJECpS`.
- Tested immutable preview: https://myproductschool-e3ixwjoci-sunboy2s-projects.vercel.app
- Moving branch preview: https://myproductschool-git-feat-platform-rebu-249507-sunboy2s-projects.vercel.app
- Evidence-only commit after that runtime: `65288992`. This handoff and any final local fix follow it; use `git log` to identify the final HEAD. Do not assume the moving alias already serves the final HEAD.
- User-owned untracked files must remain untouched: `CLAUDE.local.md`, `seeds/import2x/`.

The overall goal is **not complete**. Core implementation and many real staging journeys pass. Analytics finalization, real spoken voice, and reduced-motion verification remain open. Production has not been promoted or merged.

## Instructions and plan

Read these in order:

1. `AGENTS.md` and applicable nested instructions. Do not add Codex co-authorship to commits. Local instruction files may contain credentials: do not paste their contents into logs or handoffs.
2. Original complete web-session handoff: `/Users/sandeep/.codex/attachments/e8c8bc66-6c7b-446c-abbe-053480946d3e/pasted-text-1.txt`.
3. `docs/visual-overhaul/LOCKED_PLAN.md` — authoritative product/design plan, superseding the old rebuild sequencing.
4. `docs/visual-overhaul/INTEGRATION_STATUS.md` — chronological evidence; latest sections supersede historical blockers.
5. `docs/visual-overhaul/BILLING_PREVIEW_GATE.md`.
6. `docs/runbooks/platform-rebuild-release.md` — migration compatibility, production decision, rollback.
7. `docs/runbooks/staging-analytics-isolation.md` — exact failed-run and cleanup evidence.
8. `docs/architecture/analytics-backend-simplification.md`.

Read relevant installed Next.js guides in `node_modules/next/dist/docs/` before code changes. The project uses a Next.js version with changed APIs. Use real content and evidence; do not invent scores, catalog totals, achievements, or passed tests. Preserve the approved cream/forest/amber/sage visual system, Literata headings, Nunito Sans body, restrained motion, quiet workspaces, and contextual Hatch floating chat. Claude Code is specific to Analytics.

## What is implemented

- Claude's landing and authentication presentation from `feat/full-product-redesign` is already integrated (integration commit `522b48ba`); do not merge it again blindly.
- Home, Practice, Library, Progress, contextual Hatch, coding/SQL/canvas workspaces, readers, interviews, saved attempts, account, pricing, and billing are integrated with the approved visual system.
- Real learning data, drafts, feedback, and authentication/redirect behavior are retained.
- Stripe TEST/live mode isolation, stable customer handling, automatic-tax address collection, fenced webhook processing, duplicate/delayed delivery protection, and entitlement mirrors are implemented.
- Analytics has isolated staging compute and storage, private snapshots with checkpoint provenance, fenced provisioning/recovery, capped keys, actual gateway spend reads, immediate key blocking with retained usage records, and scheduled cleanup.
- Interview completion persists debriefs before rewards; explicit voice/chat entry and microphone preflight exist.
- Responsive canvas fitting/navigation and completed-attempt URL persistence were repaired from observed failures.

## Recent fixes and validation

### `b01d6585` — Stripe reactivation and completed-attempt URLs

Stripe rejects a reactivation request containing both `cancel_at` and `cancel_at_period_end`. The route now chooses one parameter based on the current subscription. A real TEST Keep Pro action and signed update webhook cleared cancellation correctly.

Successful coding/SQL feedback completion pins the completed `attempt` query parameter while preserving other URL state. Explicit retry clears it. On the deployed coding check:

- Challenge: `counting-distinct-senders-behind-support-ticket-aliases`.
- New attempt: `d2094b0c-1273-4fd6-a10e-d165e188dca6`.
- Python passed 5/5 visible tests and 7/7 submission tests.
- Grounded feedback: 7.4/10.
- Direct reload preserved the exact attempt, result, feedback, and expanded submitted source.

### `7e3c4d28` — immediate Settings reconciliation

Keep Pro succeeded in Stripe, but a single immediate profile refresh could read the database before its webhook arrived. Settings now applies Stripe-confirmed fields immediately and reconciles only a matching uncached profile within a ten-second deadline, including network time. New actions invalidate older reconciliation attempts.

Changed files:

- `src/app/(app)/settings/page.tsx`
- `src/lib/billing/subscription-cancellation.ts`
- `tests/lib/billing/subscription-cancellation.test.ts`

Validation: 14 focused billing tests, TypeScript, and production build passed; build generated 595 pages. Targeted lint has zero errors and two pre-existing Settings warnings. Secret scans passed. Log: `/tmp/hackproduct-build-settings-reconciliation.log`.

Real browser check passed without reload: Keep Pro + password confirmation changed ACCESS ENDS / Keep Pro to NEXT BILLING / Cancel at renewal. Exact TEST fixture `sub_1UCocKEGJUB78L7ntGwmPc7L` was cleaned up. Update event `evt_1UCoeaEGJUB78L7nzlA367xR`; deletion event `evt_1UCofSEGJUB78L7nvCAI6HQK`. Deletion processed once with cleared lease/token, no error, and zero pending webhooks. Hosted/database state returned Free, with zero blocking subscriptions.

### Other passed real journeys — retain their original candidate scope

- Standard Visa 4242 Stripe TEST Checkout completed, returned to Pro, and processed its signed completion once. Test portal rendered the correct customer/trial, scheduled cancellation, and returned to the app. Cleanup restored Free. **3DS was not tested.**
- Canvas at actual 390×844, 768×1024, and 1440×1000 browser viewports: navigation clears Hatch/footer; desktop header remains visible after inserting a two-table/one-link starter; 223-character write-up persists.
- Phone coding Run/Submit and fullscreen entry/exit pass. Saved feedback/source reopen from Submissions and Progress.
- Library search, phone contents navigation, bookmarks, and reading-position restoration pass on recorded candidates.
- SQL submission and persisted detailed feedback pass; Progress counts real completed work.
- Text interview conversation and saved debrief pass. These do not prove spoken voice.

Evidence and screenshots: `docs/visual-overhaul/evidence/staging-20260906/README.md`. These are browser viewport checks, not physical-device tests. Historical full-suite counts belong to their documented commits; do not present them as a final-HEAD suite run.

## Remaining work, in priority order

### 1. Analytics artifact generation and final-grade lifecycle — unresolved

Do not mistake the passing terminal/reconnect checks for full completion.

Fourth full run on `cf7e9fee`:

- Run `88f1e579-9507-4fe0-8431-dd565090e079`.
- Session `f7d74423-9b0f-428c-861e-31357b7dbf23`.
- Attempt `486a33b6-e489-43aa-9919-7696254d4a8f`.
- Reached real sandbox/WSS, Claude, and read-only BigQuery analysis.
- Persisted six genuine assessed checkpoints and passed reconnect continuity.
- Report existed (8,871 bytes), but expected reusable skill file was absent; no final grade.
- Gateway recorded 12 successful requests and raw spend `$0.4924225` against configured `$0.49`. No budget rejection was captured. The last accepted request crossed the limit by `$0.0024225`; strict dollar-cap compliance is therefore not proved.
- Exact cleanup passed. The harness initially misclassified the CLI wording `Cannot find revision` as incomplete cleanup; that parser was corrected without deleting unrelated resources.

Latest single recovery run on `7e3c4d28`:

- Run `03b54869-1c6a-415a-a253-03c2f670b2a1`.
- Session `7ef76e1f-6de6-4f97-8638-3c34126b6b9a`.
- Restored the prior workspace and all six passing checkpoints through the application API.
- Claude's skill-first command exited 1. No deeper safe CLI cause was captured. No new artifact was verified, no new assessment ran, and no grade was produced.
- Early spend snapshot `$0.09948`; latest preserved gateway snapshot `$0.1754103` (18 cents rounded). Keep this separate from the prior session's spend.
- Terminal state: `failed_cleanup_complete`; owned session reaped, revision absent, key blocked and retained, exact TEST subscription canceled, signed deletion processed once, Free baseline, zero blocking subscriptions.
- No run remains active. Further paid retries were stopped because of the user's time/usage concern.

Before any further execution, fix capture of the actual Claude terminal error. Do not repeat a blind canary or fabricate skill/report content to satisfy the test. Success still requires genuine report and reusable skill, all required assessments, fresh snapshots, a persisted grade, accurate spend, and exact cleanup. Preserve the failed states and compare their evidence; do not overwrite them.

Ignored helper: `.vercel/staging-analytics-canary.mjs`. The most recent executed helper SHA-256 was `4043a85c1b814c999541a1cad2b6a598fa752035d8d9a14d7a5d403ea2d4e04c`. It has ownership checks, guarded pre-mutation retries, a unique state path, and no automatic rerun after mutation. Review it before using it. It is local and untracked; another machine will need a secure local copy if this harness is to be reused.

Preserved private evidence (mode 0600):

- `.vercel/staging-analytics-canary-state-rerun-cf7e9fee.json`
- `.vercel/staging-analytics-canary-failed-88f1e579.json`
- `.vercel/staging-analytics-canary-forensics-88f1e579.json`
- `.vercel/staging-analytics-canary-state-resume-7e3c4d28.json`
- Prior failures for `ebd506fd`, `1ae7b4bc`, and earlier candidates remain alongside them.

### 2. Restarted-session accounting defect — final local correction

Final audit found that `session/start` replaces the session ID with an upsert on `attempt_id`, while `freshProvisioningState` did not reset `recorded_spend_cents` or `observed_spend_cents`. The new session inherited 49 cents despite its own gateway showing 18 cents. The atomic spend RPC subtracts the recorded baseline, so this can suppress new usage increments.

The bounded fix belongs in `src/lib/sandbox/provisioning-lease.ts` and its focused regression test. Reset the counters for a new session/key while retaining existing `usage_events`. Do not modify historical provider data or claim this also solves late, unrecorded spend on the replaced session. See the final validation note at the end of this handoff for whether the correction was committed before transfer.

### 3. Spoken voice — requires user participation

Prepared interview: `36f5455a-1905-4e87-a54e-402aaceeb955`.

Route: `/live-interviews/36f5455a-1905-4e87-a54e-402aaceeb955?autostart=1&company=Google&role=PM&discipline=system_design`.

The browser reached explicit microphone preflight. No microphone permission or speech was started. A participation question remained unanswered; do not treat general staging approval as proof of microphone participation. Verify real speech, transcription, audio response, persisted debrief, navigation/teardown, and failure fallback when the user is available.

### 4. Reduced motion and final release decision

The current browser automation did not expose reduced-motion emulation. No OS preference was changed. Test in a suitable environment and record actual evidence. Do not relabel normal animation checks as reduced-motion checks.

Keep PR #20 draft until the remaining gates are addressed or the user explicitly accepts a narrower release. Review the migration/rollback runbook before any production decision. Production merge/promotion is not authorized by the previous staging approval.

## Access and infrastructure — identifiers only, no secrets

Local credentials are in ignored files such as `.env.staging.local` (mode 0600), `.env.local`, CLI auth stores, and connector sessions. Never print or commit values. Existing access does not automatically transfer to another machine/model runtime.

### Vercel

- Project `myproductschool`, ID `prj_BnLtw2GgCcCyCnMciQ3Ps1Wezkff`.
- Team `team_gOiGgobpBZjhBPxPTkIYrSTE` / `sunboy2s-projects`.
- CLI: `npx --yes vercel`; authentication was verified.
- 31 exact branch-specific Preview environment bindings were unchanged for tested `7e3c4d28`.
- Do not overwrite production environment variables or rotate signing secrets for local convenience.

### Supabase

- Isolated staging ref `fkqsjjiunvvclwtgjqyc`.
- Production ref `tikkhvxlclivixqqqjyb` — do not confuse them.
- Staging records 181 migrations through `20260906160000`.
- Project/schema checks pass; provider branch lifecycle still reports the initial `MIGRATIONS_FAILED`. Do not call the branch lifecycle fully green.
- Approved staging database cost: `$0.01344/hour`; the branch still exists. It was not deleted or paused.
- Synthetic user ID `3fdd1b26-14b2-4af3-af13-ab173bee2354`; credentials remain local. Use only this isolated test identity for fixture work.

### Stripe TEST

- Account `acct_1PnserEGJUB78L7n`.
- Customer `cus_VD8uoGxB0kKnmM`.
- Pro monthly `price_1TbiSTEGJUB78L7nn5ePQKVo`, annual `price_1TTTaSEGJUB78L7nRkD275eQ`.
- Analytics monthly `price_1TedjCEGJUB78L7nE9tdkC6T`, annual `price_1TedjDEGJUB78L7nehEgipO1`.
- Test webhook `we_1UCibBEGJUB78L7n92aeofTh`, enabled for 16 handled events on the branch preview.
- Latest cleanup verified zero blocking/nonterminal test subscriptions. No live charges or production entitlement writes occurred.

### GCP

- Project `hackproduct`, region `us-central1`; use the local `gcloud` CLI.
- Separate services: `cc-sandbox-staging`, `cc-llm-gateway-staging`; min 0 / max 1.
- Sterile base revision `cc-sandbox-staging-00002-wm4` remains the default traffic target.
- Sandbox image: `us-central1-docker.pkg.dev/hackproduct/cc/sandbox@sha256:3e03c5c6933adbba0bb3affc0485f8287748f2eec270bb9face37ebce7e82004`.
- Gateway image digest: `sha256:a8a73e5652dde6ae32894dc7972070df250e16a4c329a61346386f4d23584b47`.
- Staging gateway uses a private LiteLLM schema/restricted login; private buckets `cc-sessions` and `cc-user-state` exist.
- Active cleanup job: `cc-reap-staging-10min` (job 3). Preserve it.
- Production Cloud SQL `cc-llm-db` last verified `STOPPED`, activation policy `NEVER`.
- Staging orchestrator `cc-staging-orchestrator@hackproduct.iam.gserviceaccount.com` has repository-only Artifact Registry reader on `us-central1/cc`, policy etag `BwZa1vsOOc0=`; no project-wide reader was added.
- Preserve other learners' tags and etag conflict handling. Do not route default traffic to a learner revision, disable budget enforcement, share uncapped Anthropic credentials, or invoke staging cleanup against production compute.
- Configured learner key budget `$0.49`, TTL 1800 seconds. App assessment/grading charges are separate; prior conservative estimate was $0.76, not a combined hard cap.

## Practical takeover checklist

1. Inspect `git status`, branch, final HEAD, and PR; preserve user untracked work.
2. Read this document and authoritative plan/evidence. Establish your own access without exposing secrets.
3. Check the final local accounting-fix note below. Do not claim it was deployed or live-tested unless new evidence proves that.
4. Diagnose the failed Claude command using useful captured output before another paid test. Avoid broad refactoring or another repeated setup cycle.
5. Complete voice and reduced-motion checks with an appropriate environment/user participation.
6. Reconcile final candidate evidence and make a production decision with the user. Existing staging approval is not a production release approval.

No agent should continue background canaries after this handoff. The current user asked to stop here and transfer the work.

## Final local accounting-fix validation

Committed as `67dde19d` (`fix: reset spend baseline for recovered sessions`). `freshProvisioningState` now resets both `observed_spend_cents` and `recorded_spend_cents` to zero for the new session/key. Its focused regression passed 6/6 tests; `npx tsc --noEmit` and the staged secret scan passed. No new production build or paid/live canary was run for this small correction. Existing usage ledger entries are retained. Late prior-session gateway spend arriving after the row is replaced remains a known limitation. The final handoff commit follows this source commit and contains documentation only.
