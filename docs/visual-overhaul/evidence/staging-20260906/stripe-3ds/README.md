# Stripe 3D Secure (3DS) checkout — September 6-7, 2026

This canary closes the "Optional 3DS challenge — untested" gap noted in
[the staging billing runbook](../../../../runbooks/staging-ui-billing-20260906.md#optional-3ds-challenge--untested).
It used only isolated Supabase staging branch `fkqsjjiunvvclwtgjqyc`, Stripe TEST
account `acct_1PnserEGJUB78L7n`, synthetic staging customer `cus_VD8uoGxB0kKnmM`
(user `3fdd1b26-14b2-4af3-af13-ab173bee2354`), and the moving branch preview
(`myproductschool-git-feat-platform-rebu-249507-sunboy2s-projects.vercel.app`).
It did not touch production Stripe or production Supabase (`tikkhvxlclivixqqqjyb`).

Both variants ran the real Stripe-hosted 3DS challenge iframe
(`testmode-acs.stripe.com/3d_secure_2_test/...../challenge`, the genuine
"3D Secure 2 Test Page"), not a stub or mock.

## Precondition

Verified before each variant: `profiles.plan = free`, `pro_access = false`, and
zero nonterminal Stripe subscriptions for the customer. The automation
(`.vercel/staging-3ds-checkout.mjs`) asserts this itself and aborts if violated
— it correctly aborted once mid-session when the decline variant was attempted
before the pass-variant subscription had been cleaned up.

## Variant 1 — 3DS authenticate and succeed (pass)

Card `4000 0025 0000 3155`, future expiry/CVC, synthetic US billing address.

| Evidence | Value |
| --- | --- |
| Checkout Session | `cs_test_b1nbsYyxTa2bLp7aOK8Pa1z6MvaTgdB1JGDj2Oi4BbKSRDLgzKiB6gJQqq` |
| Checkout state | `complete`, `paid`, TEST mode |
| Subscription (created) | `sub_1UCsSDEGJUB78L7nIbf0ajTE`, `trialing`, monthly, `price_1TbiSTEGJUB78L7nn5ePQKVo` |
| 3DS challenge | Completed via **Complete authentication** on the real Stripe test-mode ACS challenge page |
| Screenshots | [00-after-getpro-click-pass.png](./00-after-getpro-click-pass.png) · [01-checkout-embedded-pass.png](./01-checkout-embedded-pass.png) · [03-3ds-challenge-poll-pass.png](./03-3ds-challenge-poll-pass.png) · [04-3ds-challenge-clicked-pass.png](./04-3ds-challenge-clicked-pass.png) · [04-return-dashboard-pass.png](./04-return-dashboard-pass.png) · [05-settings-pro-pass.png](./05-settings-pro-pass.png) |

Checkout returned to `/dashboard?upgraded=1` (Pro badge visible in the top nav),
and Settings showed **HackProduct Pro · Monthly · Trialing**, next billing
September 13, 2026.

Signed webhook events processed exactly once each (`attempt_count=1`,
`last_error=null`, non-null `processed_at`) via the `stripe_events` ledger
added by migration `20260906120000_stripe_event_processing_state.sql`:

| Event | Type | Status |
| --- | --- | --- |
| `evt_1UCsSEEGJUB78L7nWedogdvG` | `checkout.session.completed` | processed |
| `evt_1UCsSFEGJUB78L7n7aNfe6W9` | `invoice.paid` | processed |
| `evt_1UCsSGEGJUB78L7nG8oXHWSR` | `customer.subscription.created` | processed |

Profile after: `plan=pro`, `pro_access=true`, `subscription_status=active`.

### Cleanup (pass variant)

Reverified TEST account, customer, and subscription ownership immediately
before mutation, then canceled `sub_1UCsSDEGJUB78L7nIbf0ajTE` only. Signed
deletion event `evt_1UCsTFEGJUB78L7nPqg7PMK6` (`customer.subscription.deleted`)
processed exactly once (`attempt_count=1`, `last_error=null`). Final state:

- Stripe subscription: `canceled`.
- Staging subscription row: `plan=free`, `status=canceled`, `cancel_at_period_end=false`, `canceled_at=2026-09-07T02:39:45Z`.
- Staging profile: `plan=free`, `pro_access=false`, `subscription_status=canceled`.

## Variant 2 — 3DS authenticate and fail (decline)

Card `4000 0000 0000 3220`, future expiry/CVC, run only after the pass-variant
subscription above was canceled and Free baseline was reconfirmed.

| Evidence | Value |
| --- | --- |
| Checkout Session | `cs_test_b10LZeCNE40QfBb3kLAg9VKexLamU6mLYXEIzaUlzOKWZGmx6O0Yj8JoBX` |
| Checkout state at capture | `open`, `unpaid` (never completed — challenge was intentionally failed) |
| 3DS challenge | Failed via **Fail authentication** on the real Stripe test-mode ACS challenge page |
| Subscription created | None |
| Screenshots | [00-after-getpro-click-decline.png](./00-after-getpro-click-decline.png) · [01-checkout-embedded-decline.png](./01-checkout-embedded-decline.png) · [03-3ds-challenge-poll-decline.png](./03-3ds-challenge-poll-decline.png) · [04-3ds-challenge-clicked-decline.png](./04-3ds-challenge-clicked-decline.png) · [04-checkout-error-decline.png](./04-checkout-error-decline.png) |

No `checkout.session.completed`, `invoice.paid`, `customer.subscription.created`,
or any other new Stripe event was generated by this attempt — the most recent
`stripe_events` rows immediately after this run are still the pass-variant's
events plus its cancellation, confirming Stripe never advanced the failed
Checkout Session past the authentication step. Supabase state was byte-identical
before and after:

- Profile: `plan=free`, `pro_access=false`, `subscription_status=canceled` (unchanged).
- Subscriptions table: unchanged (still the already-canceled pass-variant row; no new row inserted).

### Cleanup (decline variant)

The failed Checkout Session remained `open`/`unpaid` with no subscription
attached (Stripe's standard decline-path behavior). Reverified TEST account
and customer ownership, then expired it:
`cs_test_b10LZeCNE40QfBb3kLAg9VKexLamU6mLYXEIzaUlzOKWZGmx6O0Yj8JoBX` → `expired`.

## Final baseline verification

After both variants and all cleanup:

- All 10 Stripe subscriptions ever created for `cus_VD8uoGxB0kKnmM` on this
  TEST account: `canceled` (zero nonterminal).
- All Stripe Checkout Sessions for the customer: `complete` (2, both already
  canceled) or `expired` (8) — zero `open` sessions remain.
- Staging profile: `plan=free`, `pro_access=false`, `subscription_status=canceled`, `payment_failures=0`, `past_due_since=null`.
- No live-mode Stripe objects were touched at any point (`sk_test_`/`rk_test_` key enforced by assertion on every run).

## Summary

| Step | Result |
| --- | --- |
| 1. 3DS pass journey + screenshots | Pass |
| 2. Webhook events processed exactly once, `pro_access=true` | Pass |
| 3. 3DS decline journey, stays Free, no subscription created | Pass |
| 4. Cleanup, Free baseline, zero blocking subscriptions, no live charges | Pass |
| 5. Evidence written | Pass — this file |
