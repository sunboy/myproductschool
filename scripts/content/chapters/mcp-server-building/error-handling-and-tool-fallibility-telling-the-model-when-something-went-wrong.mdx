{"hook_text": "A tool that fails should return a structured error with a reason, not throw an exception. The model needs to know what went wrong to decide whether to retry, try a different approach, or surface the problem to the user. An unhandled exception gives it nothing to work with.", "body_mdx": "## Why Error Shape Matters\n\nThe model is not reading stack traces. When a tool call fails, the model gets whatever you put in the response. If you throw an exception that propagates to the transport layer, the model sees a JSON-RPC error object with a generic message. It has no idea whether the problem was a bad argument, a missing resource, a rate limit, or an authentication failure. It will guess, usually wrong.\n\nStructured errors give the model the information it needs to reason about what to do next.\n\n## The MCP Error Response Format\n\nA tool can fail in two ways in MCP:\n\n**Protocol-level error**: The JSON-RPC layer returns an error. Use this for transport and protocol failures, not for business logic failures.\n\n**Tool-level error**: The tool call succeeds at the protocol level but the tool reports failure in its result. This is the right place for business logic failures.\n\n```typescript\n// Tool-level error: isError: true\nreturn {\n  content: [\n    {\n      type: 'text',\n      text: JSON.stringify({\n        error: 'NOT_FOUND',\n        message: 'No file found at path: src/nonexistent.ts',\n        hint: 'Check that the path is relative to the repository root.',\n      }),\n    },\n  ],\n  isError: true,\n}\n```\n\nThe `isError: true` flag tells the model the tool did not succeed. The content carries the structured reason. The model can parse it and make a decision.\n\n## Error Categories That Change Model Behavior\n\nNot all errors are equal. The model should behave differently depending on the category:\n\n```\n┌──────────────────────┬──────────────────────────┬──────────────────────────┐\n│ Error Category       │ Model Should              │ Example                  │\n├──────────────────────┼──────────────────────────┼──────────────────────────┤\n│ BAD_ARGUMENT         │ Fix arguments, retry once │ Invalid date format      │\n│ NOT_FOUND            │ Try different identifiers │ File/resource missing    │\n│ PERMISSION_DENIED    │ Report to user, stop      │ Insufficient API access  │\n│ RATE_LIMITED         │ Wait, then retry          │ API quota exceeded       │\n│ TRANSIENT            │ Retry immediately         │ Network timeout          │\n│ CONFLICT             │ Fetch current state, retry│ Edit conflict            │\n│ UNRECOVERABLE        │ Report to user, stop      │ Service unavailable      │\n└──────────────────────┴──────────────────────────┴──────────────────────────┘\n```\n\nEncode this distinction in your error structure:\n\n```typescript\ntype ErrorCode =\n  | 'BAD_ARGUMENT'\n  | 'NOT_FOUND'\n  | 'PERMISSION_DENIED'\n  | 'RATE_LIMITED'\n  | 'TRANSIENT'\n  | 'CONFLICT'\n  | 'UNRECOVERABLE'\n\ninterface ToolError {\n  error: ErrorCode\n  message: string        // Human-readable, for the model\n  hint?: string          // What to try next\n  retryAfterMs?: number  // For RATE_LIMITED\n  details?: unknown      // Structured data the model might use\n}\n\nfunction toolError(err: ToolError) {\n  return {\n    content: [{ type: 'text' as const, text: JSON.stringify(err) }],\n    isError: true,\n  }\n}\n```\n\n## Handling Errors in Tool Implementations\n\nWrap every tool implementation so exceptions never escape to the transport layer unstructured:\n\n```typescript\nserver.setRequestHandler(CallToolRequestSchema, async (request) => {\n  const { name, arguments: args } = request.params\n\n  try {\n    switch (name) {\n      case 'read_file': return await readFileTool(args)\n      case 'write_file': return await writeFileTool(args)\n      default:\n        return toolError({\n          error: 'BAD_ARGUMENT',\n          message: `Unknown tool: ${name}`,\n        })\n    }\n  } catch (err) {\n    // Last-resort catch: log and return something useful\n    console.error(`Tool ${name} threw unexpectedly:`, err)\n    return toolError({\n      error: 'UNRECOVERABLE',\n      message: err instanceof Error ? err.message : 'Internal server error',\n      hint: 'This is a server bug. The error has been logged.',\n    })\n  }\n})\n```\n\nEach tool implementation catches its own specific errors and returns structured results:\n\n```typescript\nasync function readFileTool(args: unknown) {\n  const { path } = validateArgs(args) // throws BAD_ARGUMENT on schema violation\n\n  let content: string\n  try {\n    content = await fs.readFile(path, 'utf-8')\n  } catch (err) {\n    if ((err as NodeJS.ErrnoException).code === 'ENOENT') {\n      return toolError({\n        error: 'NOT_FOUND',\n        message: `No file at path: ${path}`,\n        hint: 'Verify the path is correct relative to the project root.',\n      })\n    }\n    if ((err as NodeJS.ErrnoException).code === 'EACCES') {\n      return toolError({\n        error: 'PERMISSION_DENIED',\n        message: `No read permission for: ${path}`,\n      })\n    }\n    throw err // unknown, let the outer handler catch it\n  }\n\n  return {\n    content: [{ type: 'text' as const, text: content }],\n  }\n}\n```\n\n## Validation Errors: Catch Them Early\n\nSchema validation happens before business logic. Use a library like Zod or ajv to validate arguments at the tool boundary:\n\n```typescript\nimport { z } from 'zod'\n\nconst ReadFileArgs = z.object({\n  path: z.string().min(1),\n  encoding: z.enum(['utf-8', 'base64']).default('utf-8'),\n})\n\nasync function readFileTool(args: unknown) {\n  const parsed = ReadFileArgs.safeParse(args)\n  if (!parsed.success) {\n    return toolError({\n      error: 'BAD_ARGUMENT',\n      message: 'Invalid arguments.',\n      details: parsed.error.flatten(),\n    })\n  }\n  // parsed.data is now typed\n  ...\n}\n```\n\nThe `details` field carries the Zod error tree. The model can see exactly which field was wrong and why.\n\n## Rate Limit Handling\n\nRate limits are a transient failure with a known retry strategy. Surface the retry window:\n\n```typescript\nif (response.status === 429) {\n  const retryAfter = response.headers.get('Retry-After')\n  return toolError({\n    error: 'RATE_LIMITED',\n    message: 'API rate limit exceeded.',\n    hint: 'Wait before retrying.',\n    retryAfterMs: retryAfter ? parseInt(retryAfter, 10) * 1000 : 60_000,\n  })\n}\n```\n\nThe model will include this context when deciding whether to retry immediately or pause.\n\n## What Structured Errors Enable\n\nWith structured errors, the model can:\n- Correct a BAD_ARGUMENT by fixing the specific field that failed\n- Try an alternative path on NOT_FOUND instead of repeating the same call\n- Report PERMISSION_DENIED to the user with a clear explanation\n- Self-throttle on RATE_LIMITED by incorporating the retry window into its plan\n- Distinguish a bug (UNRECOVERABLE) from something it can fix\n\nWithout structured errors, the model guesses. Guessing produces retry loops on unrecoverable errors, and gives up on transient errors that a single retry would have resolved.\n\nError handling is not a nice-to-have. It is half of the tool's interface with the model.\n"}
