{"hook_text": "CLAUDE.md is read at session start and shapes every decision. A well-written one is the difference between an AI pair programmer and a confused tool that needs constant correction.", "body_mdx": "## What CLAUDE.md Actually Does\n\nWhen Claude Code starts a session, it reads CLAUDE.md from the current directory (and any parent directories up to the repo root). The contents are injected into the system prompt before the user's first message. This means every file edit, every tool call, every code decision that session happens with your CLAUDE.md as standing context.\n\nThe implication is simple: CLAUDE.md is not documentation. It is a behavioral contract between you and Claude Code. Every sentence in it should describe a behavior, not explain a concept.\n\n## The Anatomy of a Useful CLAUDE.md\n\nA working CLAUDE.md has three sections:\n\n**Build and test commands.** Claude Code cannot guess these. Write the exact commands:\n\n```markdown\n## Commands\n\nnpm run dev          # Start dev server (http://localhost:3000)\nnpm run build        # Production build\nnpm run lint         # ESLint\nnpm test             # Run test suite\n```\n\nWithout this, Claude Code either invents a command (`python manage.py runserver` in a Next.js project) or asks you every time.\n\n**Architecture decisions that affect every file.** Not the full system design. The rules that affect daily edits:\n\n```markdown\n## Architecture\n\n- Use the App Router. Never create files under /pages/.\n- All styles are Tailwind inline. No separate CSS files.\n- @/* maps to ./src/*\n- Never import from a sibling module's internal path. Use the public index.\n```\n\n**Behavioral rules.** These are the constraints Claude should apply without being asked:\n\n```markdown\n## Rules\n\n- Read a file before editing it. Never assume its contents.\n- Never commit .env files or credentials.\n- Run npx tsc --noEmit after any TypeScript change. Fix errors before declaring done.\n- Prefer editing existing files over creating new ones.\n```\n\n## What Gets Ignored\n\nLong CLAUDE.md files dilute the instructions that matter. Claude Code does not have infinite attention for context. When a CLAUDE.md is 1,000 lines of background reading, the behavioral rules at line 800 get less weight than the ones at line 10.\n\nStrip anything that reads like a README:\n\n- Project history and context (irrelevant to the current task)\n- How the system works conceptually (write code comments for that)\n- Instructions for future contributors (that's a README)\n- Anything that starts with \"Note that...\"\n\nIf a sentence doesn't change how Claude Code behaves on the next edit, cut it.\n\n## Layered CLAUDE.md Files\n\nClaude Code reads CLAUDE.md files up the directory tree and merges them. This gives you a composition pattern:\n\n```\nrepo-root/\n  CLAUDE.md          ← global rules: build commands, no-go zones, style rules\n  src/\n    CLAUDE.md        ← frontend rules: Tailwind only, no raw hex in className\n    api/\n      CLAUDE.md      ← API rules: auth required on all routes, validate input at boundary\n```\n\nThe root CLAUDE.md sets the floor. Sub-directory files add specificity without repeating the globals.\n\n```mermaid\ngraph TD\n  A[repo-root/CLAUDE.md] -->|merged into system prompt| C[Claude Code Session]\n  B[src/CLAUDE.md] -->|merged into system prompt| C\n  D[src/api/CLAUDE.md] -->|merged into system prompt| C\n  C --> E[Every tool call this session]\n```\n\nThe practical use: keep database migration rules in `supabase/CLAUDE.md` so they only apply when Claude is working in that directory. Keep frontend design-token rules in `src/CLAUDE.md` where they belong.\n\n## Writing Rules That Hold\n\nWeak rule:\n> Don't use raw hex colors.\n\nStrong rule:\n> No raw hex values in JSX className strings. Use Tailwind token classes (bg-primary, text-on-surface). Raw hex is only acceptable inside style={{}} for SVG fills.\n\nThe difference: the weak rule requires interpretation. The strong rule tells Claude exactly where the constraint applies and where it does not. \"Don't do X\" almost always has exceptions. Name them explicitly.\n\nAnother pattern: attach the enforcement step to the rule.\n\n```markdown\n- Run npx tsc --noEmit after any TypeScript change. Report the output. Fix errors before proceeding.\n- Run npm run lint after adding new files. Fix lint errors before marking a task done.\n```\n\nThis turns the rule into a workflow step, not a reminder Claude might skip.\n\n## Testing Your CLAUDE.md\n\nOpen a fresh session and ask Claude Code to do something your CLAUDE.md should govern. Then check:\n\n1. Did it follow the build command?\n2. Did it stay in the right directory structure?\n3. Did it run the validation step you specified?\n\nIf not, the rule was either missing or ambiguous. Rewrite it. CLAUDE.md is a living document. The best ones are rewritten every few weeks as you discover what instructions produce consistent behavior.\n\n## Common Mistakes\n\n**Writing intent instead of behavior.** \"Keep the codebase clean\" is not a rule. \"Never create files in the root directory. Use src/ for source files, scripts/ for utilities, and docs/ for documentation\" is a rule.\n\n**Putting everything in one file.** Use the directory-scoping feature. A monolithic CLAUDE.md that governs both infrastructure Terraform and React UI components is trying to serve every context equally. It serves none of them well.\n\n**Forgetting the negative space.** The most useful rules often say what not to do. \"Never modify files in _archived/\" or \"Do not create migrations manually. Use the Supabase CLI.\" These are the rules that prevent expensive mistakes.\n\nA CLAUDE.md that takes 20 minutes to write can save hours of correction across weeks of sessions. Write it like you are onboarding a competent engineer who needs to know your specific constraints, not your entire codebase."}
