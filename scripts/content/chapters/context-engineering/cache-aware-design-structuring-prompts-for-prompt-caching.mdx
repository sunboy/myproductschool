{
  "hook_text": "Prompt caching works by hashing a prefix: move a single token anywhere in the cached region, and the cache misses entirely. You pay full price for every request, as if the cache never existed.",
  "body_mdx": "## The Cache Is a Hash, Not a Summary\n\nAnthropic's prompt caching doesn't work the way most engineers imagine. It doesn't recognize that your system prompt is \"basically the same.\" It computes a hash of the token sequence and stores the key-value computation results from the attention layers. If the hash doesn't match, there's nothing to reuse.\n\nThis means cache design is a structural problem, not a content problem. The question isn't \"what should I cache?\" It's \"which parts of my prompt are byte-for-byte stable across requests?\"\n\nThe answer, for most production systems, is more than you think, but only if you deliberately architect for it.\n\n## The Prefix Ordering Constraint\n\nAnthropic's caching hierarchy is strict: tools → system prompt → messages. Changes at any level invalidate everything below it. This isn't a quirk; it follows directly from how causal attention works. The KV cache stores attention computations for each token conditioned on everything that came before. Move anything in the middle, and the downstream attention states are wrong.\n\n```\nRequest structure (stable prefix must come FIRST):\n┌─────────────────────────────────────────┐\n│  TOOLS (rarely change)                  │ ← cache breakpoint 1\n├─────────────────────────────────────────┤\n│  SYSTEM PROMPT (static instructions)    │ ← cache breakpoint 2\n│  + reference docs / knowledge base      │\n│  + few-shot examples                    │\n├─────────────────────────────────────────┤\n│  CONVERSATION HISTORY (semi-stable)     │ ← cache breakpoint 3\n├─────────────────────────────────────────┤\n│  CURRENT USER MESSAGE (always dynamic)  │ ← no cache needed here\n└─────────────────────────────────────────┘\n```\n\nYou get four explicit cache breakpoints per request. Use them to mark the boundary between stable and dynamic content. Everything above a breakpoint is eligible for caching; everything below is processed fresh.\n\nThe implications are specific. Your system prompt must come before any user context. Reference documents go in the system block, not interpolated into the message turn. Timestamps, request IDs, and user-specific metadata must live after the last breakpoint, not embedded in otherwise-stable instructions.\n\n## What 85% Cache Hit Rate Actually Requires\n\nA real-world deployment with stable prefix structure achieved an 85.2% cache hit rate, reusing an average of 46,059 tokens per request. A structurally identical setup with perturbed prompts (minor string variations, different ordering) achieved 0%.\n\nThat 85-percentage-point gap comes down to one discipline: never mutate the prefix.\n\nConcretely, this breaks in common ways:\n\n- Interpolating the current date into the system prompt (\"Today is May 6, 2026. You are a helpful...\"). One character change, full miss.\n- Building system prompts via string concatenation where the order of appended sections varies by request path.\n- Including per-user instructions in the system block rather than in a user-turn prefix.\n- Using f-strings or template engines that introduce whitespace or punctuation differences based on context.\n\nThe fix is equally concrete: treat the cached prefix as a compiled artifact. Build it once per deployment, not per request. Validate that the string is byte-for-byte identical across requests before enabling the cache breakpoint.\n\n## The Economics Force the Architecture\n\nCache writes cost 1.25x the base input price for a 5-minute cache, or 2x for a 60-minute cache. Cache reads cost 0.1x. At $3.00/MTok for Claude Sonnet input tokens, that means:\n\n- Write a 10,000-token prefix: costs $0.0375 (1.25x of $0.03)\n- Read that prefix: costs $0.003 (0.1x of $0.03)\n- Breakeven: after 1.4 reads, every subsequent request saves 90%\n\nFor any system processing more than a handful of requests per cache lifetime, the math is simple. ProjectDiscovery, running thousands of vulnerability scans daily with repetitive system prompts, hit 59% cost reduction immediately and reached 70% after optimization. The bottleneck wasn't the model; it was restructuring their prompts so the stable scanning instructions preceded any per-target context.\n\nThe 60-minute cache is worth the 2x write cost for prompts that are genuinely stable and hit frequently. The 5-minute cache is the right default while you're still validating your hit rate.\n\n## Where Engineers Get the Architecture Wrong\n\nThe common mistake is designing for content, then trying to add caching later. System prompts accumulate user-specific context, request metadata gets prepended to \"give the model context,\" few-shot examples get shuffled based on what's \"most relevant\" to the current query.\n\nEach of these destroys cache coherence. The fix requires going back to the prompt's structure, not its content.\n\n```mermaid\nflowchart LR\n    A[Stable: tools definition] --> B[Stable: system + knowledge]\n    B --> C[Semi-stable: conversation history]\n    C --> D[Dynamic: current message]\n    A -.->|cache_control: ephemeral| A\n    B -.->|cache_control: ephemeral| B\n    C -.->|cache_control: ephemeral| C\n```\n\nThe dynamic portion, the current user message, doesn't need a breakpoint. Only the stable prefix layers do. Mark breakpoints at the boundary of each stability tier, and let everything dynamic flow through uncached.\n\nOne subtlety: thinking blocks (extended thinking in Claude's reasoning mode) can't be directly marked with `cache_control`, but they can be cached when they appear in previous assistant turns within the conversation history. Multi-turn systems that preserve reasoning history can still capture significant caching value there.\n\n## The Test Before You Ship\n\nRun two requests back-to-back with identical prefixes. If your cache hit rate in the API response metadata shows 0 after the second request, something in your prefix is varying. Add logging to serialize your prompt prefix as a string before the breakpoint and diff it across requests. The variation is almost always in something that feels harmless: a trailing newline, a formatted date, an extra space after string concatenation.\n\nFix the variation, verify the hash match, then scale. The 90% token cost reduction is real, but it's earned through structural discipline, not by enabling a flag."
}