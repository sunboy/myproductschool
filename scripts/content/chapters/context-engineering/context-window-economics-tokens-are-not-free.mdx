{
  "hook_text": "Cache reads on Claude cost $0.30 per million tokens. Fresh input costs $3.00. That 10x gap means prompt architecture is cost architecture, and most teams are paying the premium on every request.",
  "body_mdx": "## The Illusion of Cheap Tokens\n\nLLM API prices dropped roughly 80% between early 2025 and early 2026. GPT-4o input fell from $5.00 to $2.50 per million tokens. The narrative hardened: tokens are getting cheap, so don't worry about them.\n\nThat framing is wrong in two directions.\n\nFirst, token costs still vary 8x across providers. The same 1M-token workload costs $2,500/day on GPT-5.4 and $300/day on DeepSeek. For teams running at scale, that delta funds an engineer's salary. Second, token costs are at most 20-30% of total LLM deployment spend. Chips and staff account for 70-80% of production costs. Optimizing tokens while ignoring infrastructure is optimizing the wrong variable.\n\nBut the teams that actually control costs do it at the context layer. Everything else is secondary.\n\n## What Context Engineering Actually Means\n\nA prompt is not a document you write once. It's a view assembled at inference time over a larger substrate: codebases, knowledge bases, conversation history, retrieved documents. The substrate is the source of truth. The context window is what you project into that window for this specific query.\n\nThat framing shifts the question from \"how do I write a good prompt?\" to \"what information does this query actually need, and what does it cost to put that information in the window?\"\n\nAnthropic's own research found that a hybrid approach, 8k tokens of compressed conversation history plus 8k tokens of retrieved relevant context, achieves a 12.5x token cost reduction versus naive full-history approaches. Before prompt caching. The gains compound.\n\n```\nContext assembly architecture:\n\n┌─────────────────────────────────────────┐\n│           Token Budget: 16k             │\n├──────────────┬──────────────────────────┤\n│  8k: system  │  Compressed conv history │\n│  prompt      │  8k retrieved context    │\n│  (CACHED)    │  (semantic chunks only)  │\n└──────────────┴──────────────────────────┘\n        ↓               ↓\n   $0.30/M          $3.00/M\n   (cache read)     (fresh input)\n```\n\nThe left half, your system prompt, should be static enough to cache. The right half should be assembled dynamically from retrieved context, compressed history, and whatever the current turn requires. Most teams treat the whole thing as one undifferentiated blob and pay fresh input prices on every token of it.\n\n## Caching Is Not a Feature, It's a Tax You're Avoiding\n\nAnthropic prompt caching delivers cache reads at $0.30 per million tokens, 10% of the $3.00/M fresh input price. The latency benefit is equally significant: 85% reduction on long prompts. You get both simultaneously.\n\nCache writes cost 1.25x base (5-minute TTL) or 2.0x base (1-hour TTL). Breakeven requires one cache read at 5 minutes, two at one hour. Any workload where users issue multiple requests against the same system prompt, which is nearly every production application, pays back cache writes in the first request.\n\nThe pattern that works is structural: keep everything static and expensive at the top of the prompt, everything dynamic at the bottom. Models process context top-to-bottom; caching preserves exact prefixes.\n\n```mermaid\nflowchart TD\n    A[Incoming request] --> B{System prompt\\nin cache?}\n    B -- Yes --> C[\"Read at $0.30/M\\n85% latency reduction\"]\n    B -- No --> D[\"Write at $3.75/M\\n(1.25x × $3.00/M)\"]\n    D --> E[Cache stored for 5min]\n    E --> F[\"Next request hits\\nat $0.30/M\"]\n    C --> G[Assemble dynamic context]\n    F --> G\n    G --> H[Inference]\n```\n\nNetflix's overnight personalization pipeline maintained 98% cache hit rates on user profile context, contributing to $1.2M in annual API savings at 50M daily batch requests. That's not a special optimization. It's what caching achieves when the architecture is designed for it.\n\nBatch processing stacks on top: 50% discount across all token types for async workloads. Combined with caching, you reach 70-80% total cost reduction on workloads that don't require real-time response.\n\n## Position Is Half the Problem\n\nStanford and UC Berkeley published research in 2023 showing that LLMs exhibit a U-shaped attention curve. Accuracy drops more than 30% when relevant information sits in the middle of the context rather than at the beginning or end. The cause is architectural: Rotary Position Embedding, used in most modern LLMs, decays attention strength for middle tokens regardless of their importance.\n\nClaude 2.1's long-context evaluation made this concrete. On a retrieval task requiring finding relevant information in a long document, accuracy was 27% when the relevant sentence was buried in the middle. With a seven-word prompt addition (\"Here is the most relevant sentence in the context\"), accuracy jumped to 98%. Same information. Different position. Zero token cost increase.\n\nThe practical rule: put your most critical context at the beginning of the retrieved section, not wherever it happened to appear in the source document. Retrieval pipelines that return chunks in document order without reranking are silently degrading output quality.\n\nSemantic chunking improves this further. Fixed-size splits at arbitrary token boundaries separate coherent ideas across chunks, fragmenting exactly the information the model needs to reason over. Semantic boundaries, splitting where meaning actually shifts, improve RAG recall by 9% in benchmarks. The approach that performs best: encode the full document first, then pool into chunk embeddings (late chunking), rather than embedding chunks independently.\n\n## Routing: The Biggest Lever Most Teams Don't Pull\n\nIntelligent routing matches each request to the least expensive capable model. The savings range is 60-90% without sacrificing output quality on matched tasks.\n\nThe cost differences are not marginal. A support ticket triage system using Qwen 3.5B costs $72 per million requests. The GPT-4o equivalent costs $9,000. That's 125x for the same task at similar quality on a low-complexity classification problem.\n\nThe mistake is treating model selection as a one-time architecture decision. It should be a per-request decision based on task complexity. Simple classification, summarization, and template-filling tasks route to small models or cached inference. Complex reasoning, adversarial inputs, and high-stakes decisions route to larger models. A routing layer that takes 100ms to make this decision pays for itself orders of magnitude over.\n\n```mermaid\nflowchart LR\n    R[Request] --> C{Complexity\\nclassifier}\n    C -- \"simple\\n(<30% complexity)\" --> H[Haiku / Qwen 3.5B\\n$0.25-0.80/M]\n    C -- \"moderate\\n(30-70%)\" --> S[Sonnet / GPT-4o-mini\\n$1.50-2.50/M]\n    C -- \"complex\\n(>70%)\" --> O[Opus / GPT-5\\n$5-25/M]\n```\n\n## The Security Surface Nobody Priced In\n\nLarger context windows are not free risk. Every document you pull into a RAG pipeline is a potential injection vector.\n\nResearch from 2025 showed that injecting five poisoned documents into a dataset of millions achieves 90%+ efficacy in manipulating model responses without triggering detection. This is not a theoretical concern. A 2024 attack demonstrated that poisoning content sources AI systems autonomously visit could inject malicious instructions into context without any authentication requirement.\n\nWhat makes this particularly hard: Tool Poisoning Attacks corrupt tool metadata without execution, leaving no behavioral trace. Defenses that wait for the model to execute bad output miss the attack entirely.\n\nMindGuard, a decision-level guardrail released in 2025, achieves 94-99% detection precision on these attacks at under one second processing time and zero additional token cost. The defense traces each decision back to the tool that produced it, catching poisoning at the source rather than the output.\n\nThe implication for context architecture: untrusted retrieval sources require validation before they touch the context window, not after the model has already processed them.\n\n## What to Do Monday\n\nThree changes with the highest return:\n\nPin your system prompt. Make it static, move it to the top, and cache it. If your system prompt changes per user, factor out the static portion and cache that. Most teams are paying $3.00/M on context that hasn't changed in weeks.\n\nAdd a complexity router. Even a keyword-based classifier that routes obvious low-complexity tasks to a small model will cut costs 40-60% on most production workloads. Refine it over time.\n\nRerank retrieved context by relevance before inserting it into the prompt. Put the highest-relevance chunks first. This costs nothing and can match the accuracy gains of much more expensive approaches.\n\nContext window economics are product economics. The teams treating prompt structure as an engineering discipline, not a one-time setup task, are running materially different cost curves than the teams that aren't."
}