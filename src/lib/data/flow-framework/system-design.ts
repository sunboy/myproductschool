import type { Discipline } from './types'

export const systemDesign: Discipline = {
  id: 'system_design',
  name: 'System Design',
  tagline: 'How systems scale',
  tabLabel: 'System Design',
  traditions: [
    {
      id: 't1',
      glyph: 2,
      label: 'DATA-INTENSIVE',
      contribution: 'Every system trades consistency, availability, and latency',
      body: "Distributed systems are about which trade-offs you can live with. The work isn't picking the system that scales. It's picking the system whose sacrifice fits the use case. Naming the trade-off is the move that separates a designer from an implementer.",
      absorbed: ['CAP-style reasoning as default frame', 'Tunable consistency models', 'Replication as a tradeoff, not a default'],
      leftBehind: ['Specific database vendor analyses'],
      reasoningMove: "Name which two of three you're picking, and why the third is acceptable to lose.",
    },
    {
      id: 't2',
      glyph: 0,
      label: 'RELIABILITY FIRST',
      contribution: 'Reliability is engineered through embracing failure',
      body: "Systems don't become reliable by avoiding failure. They become reliable by handling failure gracefully. Designing for partial failure, error budgets, and graceful degradation is what separates systems that wake people up from systems that don't.",
      absorbed: ['Error budgets as design constraint', 'Graceful degradation paths', 'Failure modes named explicitly'],
      leftBehind: ['Site reliability engineering org structure'],
      reasoningMove: "Don't design the happy path. Design how the system breaks.",
    },
    {
      id: 't3',
      glyph: 6,
      label: 'LATENCY BUDGET',
      contribution: 'Latency is finite. Spend it deliberately.',
      body: 'Every hop costs, every cache helps, every layer of indirection extends the chain. Designing latency-aware systems is the move of allocating a budget across components and defending it.',
      absorbed: ['Latency budget allocation', 'Caching as latency tool, not just throughput tool', 'P99 over P50 as default measure'],
      leftBehind: ['Specific CDN comparisons'],
      reasoningMove: "Name the latency budget. Show where it's spent.",
    },
    {
      id: 't4',
      glyph: 5,
      label: 'CONTRACT FIRST',
      contribution: 'API contracts are the durable surface',
      body: "Implementations change; contracts don't (cheaply). The move is designing the contract first: input shape, output shape, error semantics, evolution rules. Implementation is a swap-out detail. Contracts are how systems compose without coupling.",
      absorbed: ['Contract-first design', 'Versioning strategy as design choice', 'Backward compatibility as constraint'],
      leftBehind: ['Specific protocol holy wars'],
      reasoningMove: 'Design the boundary, not the box.',
    },
    {
      id: 't5',
      glyph: 1,
      label: 'DISTRIBUTED REALITY',
      contribution: 'Distributed systems fail in correlated, surprising ways',
      body: 'The fallacies of distributed computing are still fallacies. The network is not reliable. Latency is not zero. Topology changes. Designing for distribution means assuming partial failure and correlated failures, not just independent ones.',
      absorbed: ['Plan for partial failure, not just full', 'Correlated failure modes', 'Idempotency and retries as defaults'],
      leftBehind: ['Specific consensus protocol comparisons'],
      reasoningMove: 'Failures correlate. Design like the worst case is multiple things at once.',
    },
  ],
  competencies: [
    {
      id: 'scalability_intuition',
      label: 'scalability_intuition',
      what: 'Sense for what breaks first as load grows',
      body: 'Recognizing the bottleneck before it becomes one. As load grows, something will be the constraint: CPU, IO, lock contention, network. Scalability intuition is naming which one, and why.',
      measuredIn: ['F: scale assumptions', 'O: caching strategy'],
      coaching: "You scaled the database but the bottleneck was the load balancer. What's the actual constraint?",
      failureMode: "Optimizing components that aren't on the critical path.",
      reasoningMove: 'Name what breaks first, not what scales worst.',
    },
    {
      id: 'failure_mode_reasoning',
      label: 'failure_mode_reasoning',
      what: 'Anticipating how systems degrade',
      body: "Reliable systems don't avoid failure; they degrade gracefully. The competency is enumerating failure modes: what happens when the cache dies, when the queue backs up, when the database becomes read-only. Then designing the response.",
      measuredIn: ['L: failure boundaries', 'W: failure modes addressed'],
      coaching: "You named the components but not what happens when each fails. Walk through the failure of each.",
      failureMode: 'Designing only the happy path.',
      reasoningMove: 'Design the breakage. The happy path follows.',
    },
    {
      id: 'latency_judgment',
      label: 'latency_judgment',
      what: "Naming the latency budget and where it's spent",
      body: "Latency is a budget that gets spent across the request path. The competency is allocating that budget, and noticing when a component is taking more than its share.",
      measuredIn: ['O: caching strategy', 'F: latency requirements'],
      coaching: "You added a service but didn't account for the extra 40ms. Where in the budget did that come from?",
      failureMode: 'Measuring P50 when the user feels P99.',
      reasoningMove: 'Show the latency budget. Show the spend.',
    },
    {
      id: 'api_contract_thinking',
      label: 'api_contract_thinking',
      what: 'Designing the durable interface, not the implementation',
      body: "Contracts outlive implementations. The competency is designing the interface so that the implementation behind it can change without coordination, and naming what won't change as carefully as what will.",
      measuredIn: ['F: read/write patterns', 'L: external dependencies'],
      coaching: "Your API has the right shape but no versioning story. How does this evolve?",
      failureMode: "Coupling the contract to the current implementation's shape.",
      reasoningMove: 'Design the boundary. The implementation is replaceable.',
    },
    {
      id: 'consistency_reasoning',
      label: 'consistency_reasoning',
      what: 'Choosing the right consistency model',
      body: "Strong, eventual, causal, read-your-writes: each consistency model has a cost and a use case. The competency is matching the model to the use case, not picking the strongest available by default.",
      measuredIn: ['O: consistency model', 'W: primary tradeoffs named'],
      coaching: "You chose strong consistency but the use case is fine with eventual. What does the strong consistency cost?",
      failureMode: "Defaulting to strong consistency for use cases that don't need it.",
      reasoningMove: 'Match the consistency model to the use case, not to your comfort.',
    },
  ],
  steps: [
    {
      id: 'F',
      label: 'F',
      name: 'FRAME',
      criteria: 'Scope the system and bound its constraints',
      body: "Frame for system design is bounding the problem before drawing boxes. What's the scale? Read or write heavy? Latency-sensitive? Consistency-sensitive? Frame done well makes the rest of the design defensible.",
      criteriaList: [
        { code: 'scale', title: 'scale assumptions', desc: 'Concrete numbers for read/write QPS, data size, growth' },
        { code: 'patterns', title: 'read/write patterns', desc: 'Who reads, who writes, at what frequency, in what shape' },
        { code: 'latency', title: 'latency requirements', desc: 'P50, P99, what counts as too slow for this use case' },
        { code: 'consistency', title: 'consistency needs', desc: "What inconsistency is acceptable, what isn't" },
      ],
      antiPatterns: ['Drawing boxes before naming the scale', 'Assuming consistency requirements without asking', 'Vague latency targets'],
      reasoningMove: 'Bound the problem. Numbers, not adjectives.',
    },
    {
      id: 'L',
      label: 'L',
      name: 'LIST',
      criteria: 'Enumerate components, services, and dataflows',
      body: 'List for system design is naming every component that has to exist for the system to work, plus the data flows between them. Boring middle components (message queues, feature flags, monitoring) are part of List, not optional.',
      criteriaList: [
        { code: 'comp', title: 'components named', desc: 'All major services and stores explicitly listed' },
        { code: 'flow', title: 'data flow paths', desc: 'How data moves between components, in what order' },
        { code: 'fail', title: 'failure boundaries', desc: "Where one component's failure stops vs. propagates" },
        { code: 'ext', title: 'external dependencies', desc: 'Third-party services and what their failure means' },
      ],
      antiPatterns: ['Skipping the boring middle (queues, monitoring)', 'Hand-waving over data flows', 'Ignoring external dependencies'],
      reasoningMove: 'Name everything that has to exist. Especially the boring middle.',
    },
    {
      id: 'O',
      label: 'O',
      name: 'OPTIMIZE',
      criteria: 'Trade off latency, consistency, and cost',
      body: "Optimize for system design is committing to specific trade-offs. Caching strategy, consistency model, partitioning scheme: each is a sacrifice somewhere else. Optimize done well makes the trade-offs explicit and the rationale auditable.",
      criteriaList: [
        { code: 'cache', title: 'caching strategy', desc: "What's cached, how it's invalidated, what stale-ness is acceptable" },
        { code: 'cons', title: 'consistency model', desc: 'Strong, eventual, or session, and why' },
        { code: 'part', title: 'partitioning scheme', desc: 'How data is sharded, who picks the key' },
        { code: 'cost', title: 'cost vs perf', desc: "Where money buys performance and where it doesn't" },
      ],
      antiPatterns: ['Defaulting to strong consistency', 'Treating cache as free', 'Ignoring cost in optimization'],
      reasoningMove: "Pick the trade-off. Show what you're giving up to get what you want.",
    },
    {
      id: 'W',
      label: 'W',
      name: 'WIN',
      criteria: 'Defend the architecture, surface open questions',
      body: "Win for system design is committing to an architecture that has primary trade-offs named, scaling strategy defended, and open questions explicit. Open questions aren't weakness. They're honesty about what you'd validate before building.",
      criteriaList: [
        { code: 'trade', title: 'primary tradeoffs named', desc: 'The two or three biggest sacrifices, named explicitly' },
        { code: 'scale', title: 'scaling strategy', desc: 'How the system handles 10x and 100x' },
        { code: 'fail', title: 'failure modes addressed', desc: 'What happens when each component fails' },
        { code: 'open', title: 'open questions explicit', desc: "What you'd want to validate before committing" },
      ],
      antiPatterns: ["Pretending there are no trade-offs", "Hand-waving the 100x case", "Hiding open questions"],
      reasoningMove: "Defend the architecture. Surface what you'd still want to verify.",
    },
  ],
  edges: {
    t1: ['scalability_intuition', 'consistency_reasoning'],
    t2: ['failure_mode_reasoning', 'scalability_intuition'],
    t3: ['latency_judgment', 'consistency_reasoning'],
    t4: ['api_contract_thinking'],
    t5: ['failure_mode_reasoning', 'consistency_reasoning'],
    scalability_intuition: ['F', 'O'],
    failure_mode_reasoning: ['L', 'W'],
    latency_judgment: ['O', 'F'],
    api_contract_thinking: ['F', 'L'],
    consistency_reasoning: ['O', 'W'],
  },
}
