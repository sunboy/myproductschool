# Hatch static image kit

Static raster Hatch art for the blog + newsletter (never the `HatchGlyph` SVG on these surfaces). Reused/composited from the Codex-generated pose library at `public/images/hatch/ad-poses/` (source prompts in `scripts/ad-creatives/poses.config.mjs`, brand rules in `content/autopsies/hatch-visual-style-bible.md`).

To regenerate or add poses, follow `~/.claude/skills/hatch-imagegen/SKILL.md` — never hand-draw or swap in a different art style.
